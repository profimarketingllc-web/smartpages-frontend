---
---
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SmartPages – Login & Produkte</title>

  <!-- Favicon + Fonts -->
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --navy: #1E2A45;
      --orange1: #F48C06;
      --orange2: #F77F00;
      --violet: #4234EB;
      --red: #C53A3A;
      --mint: #4ADEDE;
      --text-dark: #222;
      --radius: 18px;
      --shadow-float: 8px 8px 18px rgba(0, 0, 0, 0.1);
      --shadow-hover: 12px 12px 28px rgba(0, 0, 0, 0.15);
    }

    body {
      font-family: 'Montserrat', sans-serif;
      margin: 0;
      background: #fff;
      color: var(--text-dark);
    }

    /* HEADER */
    header {
      background: var(--navy);
      text-align: center;
      padding: 2.5rem 1rem;
      color: white;
    }

    header img {
      height: 64px;
      margin-bottom: 1rem;
    }

    header h1 {
      font-size: 2rem;
      margin: 0;
      font-weight: 700;
    }

    header p.claim {
      font-size: 1.1rem;
      opacity: 0.9;
      margin-top: 0.5rem;
      letter-spacing: 0.2px;
    }

    main {
      padding: 3rem 1rem;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .login-container {
      display: flex;
      flex-wrap: wrap;
      gap: 2rem;
      max-width: 1100px;
      background: #fff;
      border-radius: var(--radius);
      box-shadow: 0 8px 28px rgba(0,0,0,0.05);
      padding: 2.5rem;
      margin-bottom: 3rem;
    }

    .hero {
      flex: 1 1 45%;
      background: var(--navy);
      color: white;
      border-radius: var(--radius);
      padding: 2.5rem;
    }

    .hero h2 {
      font-size: 2rem;
      margin-bottom: 1rem;
    }

    .hero p {
      line-height: 1.6;
      margin: 0.5rem 0;
    }

    .form-section {
      flex: 1 1 45%;
    }

    label {
      display: block;
      font-weight: 600;
      margin: 1rem 0 0.3rem;
    }

    input[type="text"],
    input[type="email"] {
      width: 100%;
      padding: 0.9rem;
      border-radius: var(--radius);
      border: 1px solid #ccc;
      font-size: 1rem;
      transition: border-color 0.2s ease;
    }

    input:focus {
      outline: none;
      border-color: var(--orange1);
    }

    .checkbox {
      margin: 1rem 0;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.95rem;
    }

    #company_field {
      display: none;
    }

    button {
      width: 100%;
      background: linear-gradient(90deg, var(--orange1), var(--orange2));
      border: none;
      color: white;
      padding: 1rem;
      font-weight: 700;
      border-radius: var(--radius);
      font-size: 1rem;
      cursor: pointer;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    button:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-hover);
    }

    .products {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 2rem;
      max-width: 1100px;
    }

    .product-card {
      flex: 1 1 300px;
      border-radius: var(--radius);
      padding: 2rem;
      color: white;
      box-shadow: 8px 8px 16px rgba(0,0,0,0.12);
      text-align: left;
      transition: all 0.25s ease;
      transform: translateY(0);
    }

    .product-card:hover {
      transform: translateY(-8px);
      box-shadow: var(--shadow-hover);
    }

    .product-profil { background: var(--violet); }
    .product-pages { background: var(--red); }
    .product-domains { background: var(--mint); color: #003333; }

    .product-card h3 {
      margin-top: 0;
      font-size: 1.4rem;
      font-weight: 700;
    }

    footer {
      text-align: center;
      padding: 2rem;
      font-size: 0.9rem;
      color: #777;
    }

    @media (max-width: 768px) {
      .login-container {
        flex-direction: column;
      }
    }
  </style>
</head>

<body>
  <header>
    <img src="/SmartPages_icon_transparent.png" alt="SmartPages Logo" />
    <h1>SmartPages</h1>
    <p class="claim">Wir bringen dich in 10&nbsp;Minuten online – ohne Technik&nbsp;Wissen</p>
  </header>

  <main>
    <div class="login-container">
      <div class="hero">
        <h2>Willkommen bei SmartPages</h2>
        <p>Fordere deinen persönlichen <strong>Magic-Link</strong> an und starte direkt mit deinem Dashboard.</p>
        <p>Wir benötigen nur ein paar Basisdaten, damit dein Zugang korrekt zugeordnet werden kann.</p>
        <p>Du kannst wählen, ob du SmartPages privat oder als Unternehmen nutzt. Der Zugang bleibt in beiden Fällen kostenlos in der Testphase.</p>
      </div>

      <div class="form-section">
        <form id="loginForm">
          <label for="first_name">Vorname*</label>
          <input type="text" id="first_name" name="first_name" required>

          <label for="last_name">Nachname</label>
          <input type="text" id="last_name" name="last_name">

          <label for="email">E-Mail-Adresse*</label>
          <input type="email" id="email" name="email" required>

          <div class="checkbox">
            <input type="checkbox" id="is_business" name="is_business">
            <label for="is_business">Das ist ein Firmenkonto (Business)</label>
          </div>

          <div id="company_field">
            <label for="company_name">Firmenname*</label>
            <input type="text" id="company_name" name="company_name">
          </div>

          <button type="submit">Magic-Link senden</button>
        </form>
      </div>
    </div>

    <section class="products">
      <div class="product-card product-profil">
        <h3>SmartProfil</h3>
        <p>Erstelle in Minuten deine zentrale Profilseite mit Branding, Links & Kontakt – perfekt für Creator, Influencer und Solopreneure.</p>
      </div>

      <div class="product-card product-pages">
        <h3>SmartPages</h3>
        <p>Erstelle smarte Kurzlinks, tracke Klicks und optimiere deine Kampagnen mühelos – alles in deinem zentralen Dashboard.</p>
      </div>

      <div class="product-card product-domains">
        <h3>SmartDomains</h3>
        <p>Verwalte eigene Marken-Domains, sichere Redirects & SSL-Zertifikate – alles mit nur einem Klick.</p>
      </div>
    </section>
  </main>

  <footer>
    &copy; 2025 SmartPages · Profi Marketing LLC · DSGVO-konform in Europa gehostet
  </footer>

  <script>
    const checkbox = document.getElementById('is_business');
    const companyField = document.getElementById('company_field');
    checkbox.addEventListener('change', () => {
      companyField.style.display = checkbox.checked ? 'block' : 'none';
    });

    document.getElementById("loginForm").addEventListener("submit", async (e) => {
      e.preventDefault();
      const data = {
        first_name: e.target.first_name.value.trim(),
        last_name: e.target.last_name.value.trim(),
        email: e.target.email.value.trim(),
        company_name: e.target.company_name?.value.trim() || "",
        is_business: e.target.is_business.checked
      };

      const res = await fetch("https://smartpages.profi-marketing.com/api/auth/start", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(data)
      });

      const result = await res.json();
      if (result.ok) {
        alert("✅ Dein Magic-Link wurde gesendet! Bitte prüfe dein Postfach.");
      } else {
        alert("❌ Fehler: " + (result.message || "Etwas ist schiefgelaufen."));
      }
    });
  </script>
</body>
</html>
